## 2026-09-01 12:00:00Z — T-801

The adaptive cooldown floor reads the remaining budget out of the rate limit response headers, and degrades to zero when the event hook never attaches.

## 2026-09-01 11:00:00Z — T-802

A daemon thread cannot be joined after interpreter shutdown, so a worker turn has to be bounded at the process tree boundary instead of with a timeout argument.

## 2026-09-01 10:00:00Z — T-803

Integration is rebuilt by resetting to the wave base commit and replaying the kept merges in their original order, never by reverting individual commits.

## 2026-09-01 09:00:00Z — T-801

The adaptive cooldown floor reads the remaining budget out of the rate limit response headers, and degrades to zero when the event hook never attaches.

## 2026-09-01 08:00:00Z — T-802

A daemon thread cannot be joined after interpreter shutdown, so a worker turn has to be bounded at the process tree boundary instead of with a timeout argument.

## 2026-09-01 07:00:00Z — T-803

Integration is rebuilt by resetting to the wave base commit and replaying the kept merges in their original order, never by reverting individual commits.

## 2026-09-01 06:00:00Z — T-811

The supervisor evaluates the finished repository only after every wave has been accepted, which puts it outside the inner orchestrate loop.

## 2026-09-01 05:00:00Z — T-812

Artifact paths are handed to coding agents directly so they can append their own findings without a second scheduled read.

## 2026-09-01 04:00:00Z — T-813

A vendor session file is what makes resume possible, so persistence was traded back in after both adapters were found broken.

## 2026-09-01 03:00:00Z — T-814

The stub transport keeps the suite deterministic and free, because a test that reached a real command line would cost money on every run.
