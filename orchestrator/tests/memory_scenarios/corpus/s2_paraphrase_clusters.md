## 2026-09-03 12:00:00Z — T-301

The `npm ci` install failed with exit 1 because package-lock.json had drifted out of sync with package.json.

## 2026-09-03 09:00:00Z — T-302

Pytest collection aborted because an undeclared marker was used while strict marker checking was enabled.

## 2026-09-03 06:00:00Z — T-303

The Codex adapter returned an empty thread identifier, so resuming that rollout later became impossible.

## 2026-09-03 03:00:00Z — T-304

Stale git index locks remained in the worktree after a hard kill on Windows, blocking every later checkout.

## 2026-09-03 00:00:00Z — T-305

Groq reports its remaining budget through rate limit response headers, which the adaptive cooldown floor reads.

## 2026-09-05 12:00:00Z — T-301

Running `npm ci` failed with exit 1 since package-lock.json was drifted out of sync with package.json.

## 2026-09-05 09:00:00Z — T-302

Pytest collection aborted when an undeclared marker was used and strict marker checking was enabled.

## 2026-09-05 06:00:00Z — T-303

Codex adapter turns returned an empty thread identifier, so resuming that rollout afterwards became impossible.

## 2026-09-05 03:00:00Z — T-304

After a hard kill on Windows, stale git index locks remained in the worktree and blocked every later checkout.

## 2026-09-05 00:00:00Z — T-305

The remaining Groq budget arrives through rate limit response headers, which the adaptive cooldown floor reads.

## 2026-09-07 12:00:00Z — T-301

An `npm ci` step failed with exit 1 when package-lock.json drifted out of sync with package.json.

## 2026-09-07 09:00:00Z — T-302

Collection in pytest aborted since an undeclared marker was used with strict marker checking enabled.

## 2026-09-07 06:00:00Z — T-303

An empty thread identifier came back from the Codex adapter, so resuming that rollout became impossible.

## 2026-09-07 03:00:00Z — T-304

Windows hard kills left stale git index locks in the worktree, which blocked every later checkout.

## 2026-09-07 00:00:00Z — T-305

Rate limit response headers carry the remaining Groq budget that the adaptive cooldown floor reads.
