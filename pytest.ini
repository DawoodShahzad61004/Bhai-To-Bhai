[pytest]
testpaths = orchestrator/tests
# orchestrator/ on sys.path, so the bare `from config import ...` style the
# package uses at runtime resolves the same way under test.
pythonpath = orchestrator
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:orchestrator.*
