## Chronological Log

### July 2026 — Defining requirements and surveying existing orchestration tools

- **Wrote a 35-item requirements checklist:** covering multi-CLI agent support (Claude Code, Codex, Gemini CLI, OpenCode/Aider), dynamic per-stage agent selection, mid-task switching, planner→implementer→reviewer→fixer workflows, canonical cross-agent task context, native session resumption, deterministic (non-LLM-judge) completion checks, human approval gates, and honest handling of the fact that hidden model state cannot cross vendors.
- **Surveyed 10 candidate orchestration frameworks against the checklist:** Maestro Orchestrate, Claw Orchestrator, Codex Orchestrator, The Pair, Sandbox Agent, AgentOS, Agent Orchestrator, claude-codex-gemini, Session Orchestrator, CLI Continues — see `Research.md` topic 2. None fully satisfied the checklist.
- **Evaluated two more candidates, Command Code and OpenHands Agent Canvas:** ruled out Command Code (proprietary, not a cross-vendor orchestrator); found OpenHands Agent Canvas closer but still missing dynamic cross-agent routing and deterministic completion checks — see `Research.md` topic 3.
- **Adopted the 5-file markdown tracking system** (`Status.md`, `Architecture.md`, `Decisions.md`, `Research.md`, `Bugs.md`) used previously in `RAG-work`, per `Markdown-Tracking-Guide.md`.

---

#### 2026-07-31 — Requirements checklist, tool survey, and initial architecture decisions

* Set out to find — or determine whether it's necessary to build — an open-source, locally deployable platform that dynamically orchestrates existing coding-agent CLIs (Claude Code, Codex, Gemini CLI, others) through planner → implementer → reviewer → fixer workflows with a shared, canonical task context across handoffs.
* Diagnosed: no single surveyed tool (10 frameworks, plus Command Code and OpenHands Agent Canvas) fully satisfies the requirements checklist. The recurring gap across every candidate is that "multi-CLI execution/session runtime" and "dynamic cross-agent routing" are always separate concerns — no tool merges both, and none uses purely deterministic (exit-code-based) completion checks.
* Also worked out, separately, how to integrate CLI-based coding agents as LangGraph nodes (subprocess wrapping) and how to distribute a shared skill file (e.g. `graphify`) to multiple agent CLIs reliably — landed on explicit prompt injection over relying on each CLI's own auto-discovery.
* Decided (provisionally): (1) explicit skill injection over CLI auto-discovery (ADR-001); (2) target stack of Claw Orchestrator + LangGraph + MongoDB + Git worktrees + Phoenix/OpenTelemetry, reusing prior project experience rather than combining several full orchestration platforms (ADR-002); (3) keep OpenHands Agent Canvas open as an alternative to Claw Orchestrator for the execution/session layer specifically, pending hands-on evaluation (ADR-003).
* No code was written this session — this was a research and documentation session. Documented the target architecture in `Architecture.md` based on the above.
* Tracked in: `docs/Architecture.md` (initial target architecture + changelog entry), `docs/Decisions.md` (ADR-001, ADR-002, ADR-003), `docs/Research.md` (topics 1–4); no `docs/Bugs.md` entries (no code yet).

---

### August 2026 — From execution-layer research and sandbox failures to the complete production orchestrator

- **Closed the execution-layer question:** evaluated OpenHands and Claw Orchestrator hands-on rather than from documentation, reviewed Temporal for the durable-workflow slot, then adopted Maestro after Maestro-flow and Flow-next surfaced — see `Research.md` topics 5, 7, 8 and ADR-004.
- **Specified the two mechanics the headline feature depends on:** handoff-by-reconstruction (ADR-005) and the budget ledger with reactive limit detection (ADR-006).
- **Wrote the phased build order and the v0 acceptance test** into `Build-Guide.md`, closing the gap the 2026-08-03 entry flagged as recorded nowhere.
- **First code written.** Two deliberately separate tracks: `orchestrator/preflight.py` (Build Guide Stage 0) and a throwaway LangGraph hierarchical-supervisor build under `yt-tutorial/` (ADR-008) whose purpose is learning the multi-agent primitives hands-on before Stage 1 begins.
- **Maestro rescoped from a global npm install to a repo-local one** (ADR-007), which also moved 14 Claude Code hooks from global to project scope.
- **`Bugs.md` opens its first application-code entries** — one verified regression (#3) and two latent defects in the tutorial track (#5, #6) that will fire on first execution.
- **The sandbox hierarchy was made to actually run**, which turned a set of documented intentions into measured facts. Both latent defects fired exactly as written; nine more (`Bugs.md` #7–#15) were found by execution alone; four came back as decisions that apply to the orchestrator (ADR-010 through ADR-013) and four as `Research.md` topics 12–15.
- **The single most useful result is a failure that looks like a success:** a complete pipeline run that exited 0, decided `FINISH`, raised nothing, and produced no document — the concrete case behind this project's refusal to accept an agent's self-report as evidence of completion.
- **That run was then attacked across nine files**, which closed its team-handoff half outright and produced five more defects on the way (`Bugs.md` #16–#20) — every one of them a way for work to be lost with nothing reporting a failure.
- **The sandbox started driving real agent CLIs**, which is the first time anything in this repository has invoked an external coding agent: a second implementation of every supervisor and worker on the Claude Code CLI, selected by a config flag (ADR-014), with this project's own tools served to it over MCP (ADR-015), and a standalone demo running Claude Code and Codex concurrently.
- **The Stage 4 adapter layer stopped being hypothetical.** `Research.md` topic 17 catalogues nine behaviours a coding-agent CLI has that a model API does not, all found by a run failing, and `Bugs.md` #21–#23 are what they cost.
- **Consumption became readable for the first time** — and only from one vendor, which is the mixed ledger state ADR-006 has argued for on principle since 2026-08-04, now with an instance.
- **Two Windows-environment adapter regressions were closed, and vendor session resume — believed broken on Codex and believed working on Claude — was proven broken on both and fixed on both**: a Maestro-resolution and Python-environment fix (`Bugs.md` #3 follow-up, #29; ADR-022) and a session-persistence fix trading one file per dispatch for a rework loop that can actually resume the agent that made the mistake (`Bugs.md` #30, #31; ADR-021). Two more real runs were diagnosed read-only, surfacing an unborn-`master`-branch worktree failure and a browser-visible HTML defect the reviewer's checks never covered (`Bugs.md` #32, #33; `Research.md` topics 24–25).

---

#### 2026-08-03 — Maestro chosen as the execution layer; handoff and budget mechanics pinned down

* Set out to settle ADR-003's open two-way choice between Claw Orchestrator and OpenHands Agent Canvas by working with them directly instead of comparing feature lists.
* Diagnosed: the two are not really competing on the same axis. Claw's abstraction is a coordinated *group* of external agent sessions (Council, fan-out, worktree-backed parallelism); OpenHands' is a complete *environment* for running dev agents (sandboxes, UI, secrets, ACP, deployment backends). Neither implements token-aware cross-agent failover, so the core custom work is identical either way — and if the orchestration logic lives in a separate layer, Claw's coordination primitives go unused and its advantage collapses. Also corrected an earlier misreading: OpenHands *does* provide persistent conversations and multi-engine access via ACP (`Research.md` topic 5).
* Attempted a hands-on OpenHands trial against a self-hosted OpenAI-compatible LLM endpoint, with a first task chosen to exercise file creation plus deterministic verification without needing the rest of the stack running. It never reached agent execution — every request failed with `litellm.InternalServerError: OpenAIException - Connection error.` Cause not isolated; the trial was dropped when the execution layer moved to Maestro the same day (`Bugs.md` #1).
* Reviewed **Temporal** as the durable-workflow option alongside the Langflow/LangGraph question. Flagged that the provisional stack table said Langflow while ADR-002 had chosen LangGraph — these are different tools, and Langflow is a visual builder rather than a durable state-machine runtime, so it is ruled out as the engine. Detailed Temporal evaluation notes were not captured in the session transcripts and are still owed to `Research.md` topic 7.
* Learned about **Maestro-flow (Ralph)** and **Flow-next**, and found that both already do mid-task delegation to a *different* coding-agent CLI — the exact primitive the earlier 10-tool survey concluded was missing everywhere. Ralph's `cli` node type routes a chain step through `maestro delegate` as a background delegate execution; Flow-next exposes `plan-review` and `impl-review` as named commands with a swappable review backend. Ralph's decision gates are all post-execution, so a pre-execution plan-review checkpoint needs hand-assembly; Flow-next covers that natively (`Research.md` topic 8).
* **Decided: use Maestro** (ADR-004) — chosen for its general-purpose `delegate` primitive, Session/Run lifecycle, and knowledge system, and because it is already installed and configured here. Flow-next is the more configurable of the two out of the box and is retained as the fallback. This withdraws Claw Orchestrator from ADR-002 and resolves ADR-003 in the negative for both of its candidates.
* Also decided two mechanics that everything else rests on: handoff packets are **reconstructed on entry** from continuously-written durable artifacts rather than summarized on exit (ADR-005) — the naive summarize-on-exit design fails precisely when an agent is killed by a rate limit, which is the case the whole feature exists for. And agent budgets are modelled as a **configured ledger with reactive limit detection as the authoritative signal** (ADR-006), because no target agent exposes remaining allowance and the four are not bounded by comparable quantities.
* Environment work: installed Claude Code v2.1.220 and fixed its PATH (`Bugs.md` #2); created a project virtualenv on **Python 3.13.13** via `uv`, moving off the locally installed 3.14 because the agent/LLM package ecosystem lags new interpreter releases and several intended dependencies ship compiled wheels.
* No code was written this session. Build guidance and a phase-scoped dependency list were produced during the session but **were not saved into the repository** — `docs/Build-Guide.md`, `requirements.txt`, and `requirements-dev.txt` do not exist in the working tree or in git history. Their substance is preserved in ADR-005, ADR-006, `Research.md` topics 6–8, and the `Architecture.md` changelog; the phased build order and the concrete v0 acceptance test are **not** yet recorded anywhere and should be rewritten before implementation starts.
* Target for v0, worth restating since it is the clearest definition of done: give the system a real task in a real repo, set one agent's budget artificially low so it will be exhausted partway through, and have the system plan, start implementing, detect the limit, fail over to a second agent mid-task, continue from the existing partial work rather than restarting, pass the deterministic check runner, and leave a complete audit trail.
* Tracked in: `docs/Decisions.md` (ADR-004, ADR-005, ADR-006; status updates on ADR-002 and ADR-003), `docs/Research.md` (topics 5–8), `docs/Architecture.md` (stack table, Agent Router, Handoff Builder, changelog entry), `docs/Bugs.md` (entries 1–2).

---

#### 2026-08-04 — Maestro rescoped to a repo-local install; Build Guide written; Stage 0 preflight built; LangGraph tutorial track opened and a hierarchical supervisor graph brought to compile

* Set out to do three things: record the phased build order and v0 acceptance test that the previous session produced but never saved; undo an unintentional global Maestro install so the dependency is scoped to this repository; and — having concluded that no further design work substitutes for never having built a multi-agent system — start a deliberately throwaway LangGraph tutorial build to learn the primitives hands-on. The tutorial work ran on past midnight; all of it is recorded here as one session.
* Diagnosed, on the Maestro scoping request: the ask was "put Maestro in this repo's uv," but `maestro-flow` is an npm package with no PyPI equivalent, and the repo's `.venv` has no `pyproject.toml` or `uv.lock` — it is a bare venv, not a uv project. The repo-local equivalent is a local `npm install`, not anything uv can express. Separately, what actually made Maestro *global* was not the binary but **14 hooks plus a statusline registered in `~/.claude/settings.json`**, which fired in every project opened on this machine; those hooks are what had silently created `.workflow/` cache directories in both this repo and the sibling `Resume` project. "Where the binary lives" and "where the hooks run" turned out to be two independent decisions.
* Migrated accordingly (ADR-007): `npm install maestro-flow@0.5.61` into the repo; the 14 hooks and the statusline rewritten into `./.claude/settings.json` pointing at `$CLAUDE_PROJECT_DIR/node_modules/.bin/maestro`; the hook block then stripped from global settings (backed up to `~/.claude/settings.json.bak-20260804`) and the global package uninstalled. `~/.maestro/` was deliberately left in place, since the global `CLAUDE.md` `@`-imports `cli-tools.json` and `delegate-usage.md` from it. The replacement was proven before the original was removed — version probe, a real `hooks run session-context`, the statusline, `search`, and `delegate show` were all exercised against the local binary first, and the exact `$CLAUDE_PROJECT_DIR` command strings simulated. The orphaned search daemon (PID 39464) was stopped. npm 11 gated 5 native install scripts (`onnxruntime-node`, `@zvec/zvec`, `argon2`, `sharp`, `protobufjs`), so search falls back to BM25-only until `npm install-scripts approve` is run; delegate, hooks, and the statusline are unaffected.
* Wrote `Build-Guide.md` (249 lines) — the phased build order and the concrete v0 acceptance test that the 2026-08-03 entry flagged as recorded nowhere. It derives six working habits from the sibling `RAG-work` project's seven-month log, decomposes the v0 target into eight testable assertions (A1–A8), singles out **A4** — a handoff packet built with the outgoing agent having written nothing on exit — as the assertion that actually tests the design, and lays out Stages 0–8. A machine audit run the same day scoped v0 to **Codex + Claude Code**: `gemini`, `opencode`, and `aider` are not installed, and `~/.maestro/cli-tools.json` has only `codex` (and the read-only `api-explore`) enabled.
* Built Stage 0's first artifact: `orchestrator/preflight.py` (297 lines) — resolves each enabled agent binary from `cli-tools.json` (workspace entries overriding global), runs a `--version` probe on each, pings MongoDB, and confirms `git worktree` support, exiting non-zero with a per-item report. This is the startup preflight `Bugs.md` #2's Prevention note asked for. It now fails on its own Maestro check as a direct consequence of the same day's local-install migration (`Bugs.md` #3).
* Opened a second, deliberately separate track (ADR-008): a LangGraph hierarchical-supervisor build under `yt-tutorial/`, following a 52-minute multi-agent walkthrough. The rationale is habit 5 from the Build Guide — `RAG-work` sandboxed LangGraph in a throwaway file before adopting it — with the added reason that prior agent experience here is a single-agent RAG system (Memora), not a multi-agent one. Started with `yt-tutorial/tools.py`: `scrape_webpages` via `UnstructuredLoader`, plus `create_outline` / `read_document` / `write_document` / `edit_document`, all scoped to a `temp/` directory.
* Built the research team's half of the hierarchy: `state.py` (a `State(MessagesState)` plus a general-purpose `make_supervisor_node(llm, members)` factory returning a supervisor that routes with `Command(goto=..., update=...)`), `search_node.py` (two `create_agent` workers — `search_agent` over Tavily and `web_scraper_agent` over `scrape_webpages` — each wrapped in a node that returns control to the supervisor), and `research_builder.py` (assembles the `StateGraph`, compiles it, renders `research_graph.png`). Added `tavily_tool` to `tools.py`.
* Six errors were worked through to get there, and they cluster into one pattern rather than six independent faults: **tutorial material written against an older LangGraph/LangChain has drifted from the installed 1.x APIs.** `GraphState` does not exist and was a transposition of `StateGraph`; `create_react_agent` from `langgraph.prebuilt` is now `create_agent` from `langchain.agents`; the end sentinel is the exported `END` constant, and hand-writing `'_end_'` produces `ValueError: Found edge ending at unknown node`; `draw_mermaid_png()` needs no `mermaid` PyPI package (and the one that exists has unparseable metadata); `Path` comes from `pathlib`. A missing `return supervisor` at the end of the factory produced a bare `RuntimeError` from `add_node`, since the node was `None`. Recorded as `Bugs.md` #4 and `Research.md` topic 10.
* Ported four modules from `RAG-work` rather than writing fresh plumbing (ADR-009): `llm_caller.py` (926 lines — a FIFO call gate with per-thread events for strict arrival-order fairness, a global token-reset deadline so a 429's reset window is paid exactly once per exhaustion event, an adaptive inter-call cooldown derived from rate-limit headers, retry/backoff, and JSON repair), `logger_config.py` (265 lines), `config.py` (retry limits, backoff bounds, timeouts, cooldown bounds), and `llm_setup.py` (four `ChatOpenAI` clients — `llm`, `llm_tool`, `judge_llm`, `json_fix_llm` — against a custom OpenAI-compatible base URL, with the Groq versions commented out). Deleted `setup.py`, whose contents were folded into `config.py`.
* Worth flagging as more than tutorial trivia: `llm_caller.py` already implements, at the LLM-API level, the half of ADR-006 that matters most — reactive rate-limit detection as an authoritative signal, with a cooldown and a shared reset deadline. The budget ledger needs the same shape one layer up, at the agent-CLI process boundary. Recorded in `Research.md` topic 11.
* Two latent defects were identified by reading rather than by running, because **the graph has only ever been compiled, never invoked**. `state.py:26-27` calls `llm_invoke(llm, messages)` and subscripts the result as `response["next"]`, but `llm_invoke` returns an `LLMResult` dataclass, which is not subscriptable; the structured-output binding (`.with_structured_output(Router)`) and the `Router` TypedDict the supervisor depends on are absent entirely (`Bugs.md` #5). And `llm_caller.py`'s adaptive cooldown derives its floor from Groq's `x-ratelimit-*-tokens` response headers captured through an httpx hook that only attaches to a Groq client — against the `ChatOpenAI` clients now configured, the hook silently declines to attach and the cooldown floor degrades to zero (`Bugs.md` #6). The error taxonomy handles both vendors; only the header-derived cooldown is Groq-coupled.
* Also this session: `graphify` (v0.8.28) was run over the repository, producing `graphify-out/` with `graph.json`, `graph.html`, and `GRAPH_REPORT.md`; and `docs/Build-Guide.md` was moved to the repository root as `Build-Guide.md` (content identical).
* Tracked in: `docs/Decisions.md` (ADR-007, ADR-008, ADR-009; status update on ADR-006), `docs/Research.md` (topics 9–11), `docs/Bugs.md` (#3, #4, #5, #6), `docs/Architecture.md` (Implementation Status section, stack table, changelog entry), `Build-Guide.md`, `orchestrator/preflight.py`, `yt-tutorial/`.

---

#### 2026-08-05 — The tutorial hierarchy completed and made to run; nine defects found by execution; the sandbox reorganized into packages with config, prompts, and per-run logging

* Set out to finish the hierarchical multi-agent build started the previous session — the writing team, the top-level orchestrator over both teams, and a first real invocation — then, once it ran, to reorganize it into something readable and to wire up the logging module that had been sitting unused. Two commits: `258e04e` *Tutorial code completed* and `5e4fb11` *Organized, cleaned, logging added, config added*.
* **Completed the hierarchy.** `writing_team.py` (three workers: `doc_writer` over write/edit/read, `note_taker` over outline/read, `chart_generator` over read plus a Python REPL, and their supervisor), `writing_builder.py`, and `super_builder.py` — the top-level graph routing between `research_team` and `writing_team`, each a compiled sub-graph invoked from a node. `main.py` streamed the first real request through it. Both teams reuse the same `make_supervisor_node` factory, which is the piece the whole exercise was for.
* **Both latent defects fired, and reading had missed a third.** `Bugs.md` #5 raised its predicted `TypeError: 'LLMResult' object is not subscriptable` on the first invocation; fixed with a `Router` TypedDict bound through `.with_structured_output(...)`, passed *through* `llm_invoke` so the supervisor keeps the FIFO gate, plus the `result.ok` check the original entry had also flagged. The third defect surfaced only while fixing it: `State` had no `next` key, so the supervisor's `update={"next": goto}` had **always been a no-op** — LangGraph silently discards updates to channels absent from the schema, verified with a minimal two-node graph. An unknown edge target is a compile-time error; an unknown state key is not an error at all. `Bugs.md` #6 fired too, and exactly as silently as its own Symptom field predicted (below).
* **Diagnosed the local serving stack by probing it rather than trusting "OpenAI-compatible."** The endpoint is HuggingFace TGI `3.0.0-sha-8f326c9` serving `llama-3.1-8b-instruct`. A capability probe found plain chat 200, `response_format` json_object **500**, json_schema **500**, tool calling 200 — and json_schema is what `langchain-openai` 1.4.1 sends by default from `with_structured_output()`, so the library's default was the one route the server does not implement (`Bugs.md` #8; fixed with `STRUCTURED_OUTPUT_METHOD = "function_calling"` in config). Four further deviations sit in the wire format itself: `arguments` returned as a JSON object rather than a string, `content` omitted on tool-call turns, every tool call labelled `id: "0"`, and — bisected with five message shapes differing only in that field — HTTP **500** for any assistant message with `content: null`, which is exactly what langchain re-serializes an empty content field into on the next turn (`Bugs.md` #7).
* **Wrote `tgi_compat.py`, a 142-line `httpx` transport shim** normalizing all of it in both directions, and put it on the transport rather than in the agents (ADR-010). The first fix attempt patched the supervisor and would have left all five workers broken one turn later, which is the argument in miniature: the divergence is per-request, not per-caller. Verified by A/B on an identical payload against the live server — default transport 500, compat transport 200 — and against a stub reproducing TGI's exact captured bytes.
* **Established that the loop could not be closed by prompting.** A controlled A/B sent the same conversation twice, differing only in whether `tools` was in the request: with tools, another tool call and empty content; without, a complete finished answer as text. TGI grammar-constrains generation to the attached schemas, so while tools are bound the model **structurally cannot** produce the assistant-turn-with-content-and-no-tool-calls a ReAct loop exits on. An explicit `finish_work` tool was tried and the model called `write_document` instead. Bounded it with `ToolCallLimitMiddleware(run_limit=3, exit_behavior="end")` — and the loop **moved** rather than closing, because the middleware's terminal stub (`Tool call limit reached: …`) was being handed to the supervisor as the worker's report, where it reads as unfinished work. `agent_report()` now replaces that stub with what the worker actually did (`Bugs.md` #9, ADR-011).
* **Then the same pattern one level up.** The research team looped on a request asking it to "compile in a file named `research_results.txt`" — it has no file-writing tool, its search worker satisfied the first clause on pass one, and the supervisor re-routed forever chasing a clause it structurally could not complete. Adding a `scope` sentence to the supervisor prompt telling it so **did not work**; the 8B router ignored it. A structural repeat cap (`MAX_WORKER_REPORTS`) did. The scope prompts were kept because they are correct and should help a stronger model, and documented in-code as not being the mechanism (`Bugs.md` #13).
* **Fixed a sandbox escape that was also the engine of a rewrite loop.** The document tools built paths as `os.path.join(cwd, "temp", file_name)` and returned the **absolute** path; `os.path.join` discards its base when the tail is absolute, so the tool announced a path, the model echoed it back as `file_name`, and the resolver honoured it — a perfect fixed point, and a write-anywhere-on-disk hole in the same line. Now `basename`-sandboxed, returning bare filenames (`Bugs.md` #10).
* **Three more tool-layer defects that only execution could surface** (`Bugs.md` #11): `python_repl_tool` referenced an undefined `repl` and its own `except Exception` had been converting the resulting `NameError` into a returned string, so the tool had never worked and had never looked broken; bare `open()` on Windows defaults to cp1252 and died on an em dash in model output (`UnicodeEncodeError: 'charmap' codec…`) *after* a correct tool call; and `read_document`/`edit_document` raised on a missing file, so one hallucinated filename killed an entire graph run, while `python_repl_tool` in the same file returned its errors as text. All four file operations are now `encoding="utf-8"`, and the document tools report errors with a listing of what is actually in the workspace so the agent can self-correct.
* **`recursion_limit` is a step budget, not a nesting depth, and sub-graphs inherit it.** `{"recursion_limit": 2}` at the entry point surfaced as `GraphRecursionError` raised *inside* `research_graph.invoke()`, which had been called with no config of its own — the level that exhausts a shared budget is not the level that misconfigured it. Fixed with explicit per-level allocation (`SUPER_GRAPH_RECURSION_LIMIT = 50`, `TEAM_CONFIG` at 25). The same call site was also passing a single message where a list is contracted; `add_messages` had been coercing it, which made it work by accident (`Bugs.md` #12).
* **Reorganized the sandbox into packages** (ADR-012): `llm/` (`clients.py`, `invoker.py`, `tgi_compat.py`), `tools/` (`documents.py`, `web.py`, `code.py`), `teams/` (`state.py`, `supervisor.py`, `research.py`, `writing.py`, `orchestrator.py`, `visualization.py`) over `main.py`, `config.py`, and `prompts.py`. Six modules collapsed — each `*_builder.py` merged into its team module, `state.py` split into pure state and the supervisor factory. `config.py` grew from 20 to 83 lines and became the single source of tunables, each non-obvious constant carrying *why* it holds that value, because several of them are the findings above: `TOOL_CALL_RUN_LIMIT = 3`, `MAX_WORKER_REPORTS = 2`, `STRUCTURED_OUTPUT_METHOD = "function_calling"`, and `LLM_MAX_RETRIES = 0` (the invoker owns retry policy; an SDK retrying underneath would hide the 429s its FIFO gate exists to handle). Prompts went to a separate module because they change for different reasons than tuning constants. Behaviour was held fixed: both team graphs were re-run and routed identically, and a supervisor's fully assembled prompt was reconstructed after the extraction and compared byte-for-byte.
* **The ported `logger_config.py` turned out never to have executed.** Its first real call raised `NameError: name 'tracing_handler' is not defined` — two handler variables were referenced and never constructed, neither constructible here since `langfuse` and `openinference` are absent, and about half the file imported `app_workflow.*` packages belonging to `RAG-work`. Undetected for a day because nothing imported it. Replaced rather than repaired, 265 lines in and 132 out, keeping the `ContextVar` request-id filter, the dual console/file handlers, and the muted-library list (`Bugs.md` #14, ADR-013). This is deliberately the opposite call to ADR-009's port-as-is, and the distinction that emerged is: **port what is expensive to re-derive, rewrite what is cheap.**
* **Instrumented the pipeline and got one `run_logs/<app>_<timestamp>.debug.log` per run** — console at INFO, file at DEBUG, every supervisor decision, worker entry and report, team entry and exit, and every tool operation with sizes. It paid for itself immediately, and produced the session's most important finding.
* **The full pipeline now runs end to end and terminates cleanly — and its most recent complete run produced nothing.** `agent_run_20260805_163729.debug.log`: five steps, no exception, exit 0, `FINISH`, and `Run finished | steps=5 | documents=none` against a request that asked for a file. The top-level supervisor routed to `research_team`, got its report, routed to `research_team` **again** rather than to `writing_team`, and on the third selection hit the repeat cap, which forced `END`. `>>> writing_team starting` never appears. The research team's closing report even asserts the results *"can be compiled in a file named 'research_results.txt'"* — a completion claim for an action nothing performed (`Bugs.md` #15). So the guard introduced to bound a loop also acquired the power to abandon a task, logging the same line for both. Every completion signal the system emits about itself agreed, and all of them were wrong; only comparing artefacts against the request revealed it.
* **The same log confirmed `Bugs.md` #6 and extended it.** `could not extract rate-limit headers — cooldown base will be 0` on 9 of 9 supervisor calls, plus `token usage — input=n/a output=n/a total=n/a` on all 9 — so against this endpoint the *proactive accumulation* half of ADR-006 has no data source either, not just the reactive cooldown. Both halves of the ledger analogue are inert, silently, and the runs look entirely normal (`Bugs.md` #6a). Recorded as a status update on ADR-006: `available` computed from no data must be a distinct, visible ledger state.
* No orchestrator-track code was written this session; `Bugs.md` #3 (the preflight's Maestro probe and the missing `requirements.txt`) remains open and untouched.
* Tracked in: `docs/Bugs.md` (#5a, #6a, #7–#15), `docs/Decisions.md` (ADR-010, ADR-011, ADR-012, ADR-013; status updates on ADR-006, ADR-008, ADR-009), `docs/Research.md` (topics 12–15), `docs/Architecture.md` (Implementation Status tree and mapping table rewritten, stack table amended, changelog entry), `README.md`, and `yt-tutorial/` across commits `258e04e` and `5e4fb11`.

---

#### 2026-08-06 — The clean-run-that-produced-nothing attacked across nine files; a second implementation of every agent on the Claude Code CLI; two vendor CLIs run concurrently

* Set out to do one thing and ended up doing three. The first was to fix `Bugs.md` #15 — the run that exits 0, decides `FINISH`, raises nothing and produces no document. That work then made the second worth doing: with the mechanical defects cleared, the remaining failure was routing *judgement*, which an 8B model could not supply and which no amount of code could establish was even the problem while there was only one model in the system. So every supervisor and worker got a second implementation on the Claude Code CLI, selected by a flag. The third — a tiny standalone demo running Claude Code and Codex at the same time — came out of the second. Three commits: `ef6b559` *Enhance LLM client and invoker with timeout handling and frequency penalty*, `6417429` *Integration of Claude Code Based Agents*, `0632e4a` *Running Claude Code and Codex Agents Concurrently*.
* **The assault on #15 (668 insertions, 107 deletions, nine files).** Its team-handoff half is now **fixed outright**: `teams/orchestrator.py` passes the whole conversation into a team rather than `messages[-1]`, so a team invoked a second time is no longer handed its own previous report to re-research, and the writing team finally sees the original request including the file name it is supposed to produce. New `team_report()` folds back every message a team added rather than only its closing one, which after a forced FINISH is often the thinnest. `agent_report()` was rewritten to search **backwards** for the last real AI message instead of reading the middleware's terminal stub, and now appends a `Tools executed:` line — because prose is only a claim, and a worker saying it wrote a file versus a `write_document` call in the transcript are different kinds of evidence. When there is no prose anywhere, the new `_tool_results()` salvages the raw tool output, since otherwise the next team has nothing to write about but the request itself. `tool_call_limit` switched from `exit_behavior="end"` to `"continue"`, so a worker that spent its whole budget searching still gets a turn to report. The routing half is **not** fixed and is not fixable in code; `prompts.py` (342 lines changed) gained an explicit four-step DECISION PROCEDURE, an EVIDENCE RULE ("CAN be done, SHOULD be done and READY to be done all mean OPEN"), a REPETITION RULE, and per-scope GOOD/BAD examples naming the exact failure modes from #13 and #15 — a mitigation, not a guarantee.
* **Five defects found on the way there, all of the same family: work lost with nothing reporting a failure.** A **repetition loop inside a grammar-constrained tool argument** — near-greedy decoding re-emitting the same URLs with the escaping doubling each pass until `max_tokens` ran out mid-string, which the server rejects as `EOF while parsing a string` after two minutes of billed generation (#16; `LLM_FREQUENCY_PENALTY = 0.4`, plus removing the `Sources:` line that had put one URL four times into the writer's context). **Two timeouts that structurally could not fire** — `LLM_RESPONSE_TIMEOUT_SECONDS` is a *per-read* timeout that a server holding the socket open never trips, measured on a turn that ran **388 s**, and it was not even reaching workers, since `create_agent` bypasses `llm/invoker.py` entirely and the effective value was the SDK's 600 s default; separately `_invoke_once`'s `ThreadPoolExecutor` context manager blocked on `shutdown(wait=True)`, i.e. on the exact call the timeout existed to escape (#17; `WORKER_DEADLINE_SECONDS = 180`, wall-clock, over a whole turn). **The run printed its summary and the process never exited** — `concurrent.futures` registers an `atexit` hook that joins its non-daemon threads and `shutdown(wait=False)` does not opt out, so every abandoned turn kept the interpreter alive; reproduced in five lines outside the project, fixed with a daemon thread in the new `run_with_deadline()` (#18). **One worker's 422 aborted the whole graph**, discarding a completed research report and a 1,459-character document that were already on disk — because the invoker's full error taxonomy never sees a worker turn (#19; new `run_worker()`, which always returns a report: *a supervisor can route around a worker that failed; it cannot route around a traceback*). And the **document tools let the model's habits reach disk** — literal `\n` everywhere lines should break, and the note taker writing its outline over the deliverable's file name because the request names that file and nothing else (#20; `_unescape()`, plus two self-correcting guards on `create_outline` ordered name-first, since when the note taker runs before the writer the deliverable does not exist yet for a content guard to protect).
* **A second implementation of every node, on the Claude Code CLI** (1,147 insertions, ADR-014). The design constraint is stated up front in `claude_agents/__init__.py`: every node is interchangeable with the one it replaces — same node name, same state in, same `Command` out, same report format — and the graphs in `teams/` are unchanged. `USE_CLAUDE_CODE_AGENTS` picks the implementation; `CLAUDE_CODE_SEND_CUSTOM_TOOLS` picks whether Claude Code gets this project's tools over MCP or uses its own (ADR-015). Briefs are imported **verbatim** from `prompts.py` and no role was rewritten; `claude_agents/prompts.py` adds framing only — that this is an automated pipeline rather than a person, that the turn ends the moment it replies, and that it should ignore the repository conventions it would normally pick up. `cli.py` is the single place the CLI is invoked, with `ClaudeResult` deliberately mirroring `LLMResult` and never raising at the boundary; two flags carry the isolation, `--tools ""` (built-ins off without touching MCP tools) and `--strict-mcp-config` (ignore every MCP server configured anywhere else), which together leave exactly the tools a run intends. `router.py` passes the same `Router` schema as `--json-schema` and delegates everything except two things: the router gets no tools at all, and the repeat guard stays, because it is all that stands between a supervisor that will not stop and a run that bills a paid CLI every turn until the recursion limit.
* **The immediate return was diagnostic, and it is the reason the second commit was worth writing.** The same graph, the same prompts, and the same request — the one that had been ending `documents=none` — routed research → writing → FINISH correctly and produced a grounded five-tool `research_results.txt`. Five steps, 94 seconds, **$1.05 across 9 CLI calls**. That separates "the topology is wrong" from "this model is too small" with evidence rather than assertion, which the single-model sandbox structurally could not do.
* **Three defects that only exist because an agent CLI is not a model API.** `doc_writer` reported *"Wrote _cc_test.txt: 2 open-source grammar checkers…"* — fluent, specific, consistent with the request, and false, because Claude Code attaches `--mcp-config` servers **asynchronously and does not wait**, and the bridge takes ~3.3 s to import langchain, so the turn was planned against no tools at all (#21; `alwaysLoad: true` puts the connection on the blocking path). That is `Bugs.md` #15's failure mode recurring one substrate up and caught the same way — by checking the filesystem. `chart_generator` then reported saving a PNG that had gone to Claude Code's **own session scratchpad**, because its system prompt names that directory by absolute path while the role brief said "the workspace directory" (#22) — and that was undiagnosable until an `AGENT_RUN_LOG` env var pointed the bridge subprocess at the parent's log file, since Claude Code keeps an MCP server's stderr for its own diagnostics and never forwards it.
* **Two agent CLIs running concurrently** (`temp_agents/`, deliberately tiny: one supervisor, two agents, two files, one on each backend, chosen by config, sharing nothing with `teams/` or `claude_agents/`). The value is entirely in what the Windows/Codex integration cost, and all of it is recorded in comments. The prompt goes over **stdin, always** — the npm-installed `codex` is a `.cmd` shim, `cmd.exe` re-parses its arguments, and a multi-line prompt arrives truncated at its first newline; the first run lost the "Hi World!" line that way and codex, believing it had been asked for an empty file, faithfully produced one. Errors are read from the **last** `ERROR:` line rather than the head of stderr, because codex prints a banner before doing anything, which is how a rejected model spent a run looking like a mysterious empty result. `CODEX_MODEL` defaults to empty, since naming `gpt-4o` got every run rejected with *"not supported when using Codex with a ChatGPT account"*. The reply comes from `--output-last-message`, because `codex exec` streams its reasoning to stdout with no marker around the final answer. And there is a byte-order-mark clause, because codex writes files on Windows through PowerShell, which prefixes UTF-8 with a BOM — so `Hi World!` arrived on disk as `﻿Hi World!` and the supervising model read it, understood it, and approved it, being unable to see the property in question (#23).
* **Two design details in that demo are the ones worth keeping.** `temp_agents/supervisor.py` submits both agents before collecting either result — which is what makes them run at the same time — using threads rather than processes because each agent is a `subprocess.run` blocked on a pipe, which releases the GIL. And `temp_agents/main.py` deletes both target files up front, *so the supervisor's check means something*: a file left over from an earlier run would pass while this run wrote nothing. That is `Bugs.md` #15's lesson applied preemptively, and it paid off in the same session, since the first codex run wrote a 0-byte file.
* **Also this session:** `yt-tutorial/` → `yt_tutorial/`, every file moved with zero content changes — the hyphen is not a legal Python package name and is what blocked `python -m temp_agents.main`. The rename then disabled the sandbox twice over, both silently: an editor rewrote seven imports to a `yt_tutorial` package that does not exist, and a shell activated *before* the rename fell through to the Microsoft Store `python.exe` stub, which prints nothing and exits 9009 — two identical silent runs during which no Python executed at all (#24). `CLAUDE_CODE_MODEL` was also dropped from `sonnet` to `haiku`. Both demo output files are committed and byte-verified: `file_A.txt` = `Hello World!`, `file_B.txt` = `Hi World!`.
* **One discrepancy found while writing this up and left open:** `USE_CLAUDE_CODE_AGENTS` defaults to `True` while the comment directly above it says the flag is off by default so the LangChain path "stays the one that works out of the box" (#25). A fresh clone therefore runs the whole pipeline through the paid CLI, at a measured $1.05 per run, and fails outright without a subscription. Not changed here — which of the two is wrong is a call for the author.
* No orchestrator-track code was written this session; `Bugs.md` #3 (the preflight's Maestro probe and the missing `requirements.txt`) remains open and untouched, and is now the only thing standing in `orchestrator/` while the sandbox holds the only working agent-CLI adapters in the repository.
* Tracked in: `docs/Bugs.md` (#16–#25, follow-up on #15), `docs/Decisions.md` (ADR-014, ADR-015, ADR-016; status updates on ADR-006, ADR-008, ADR-011, ADR-013), `docs/Research.md` (topics 16–18), `docs/Architecture.md` (Implementation Status tree and mapping table extended, stack table amended, changelog entry), `README.md`, `graphify-out/`, and `yt_tutorial/` across commits `ef6b559`, `6417429`, and `0632e4a`.

---


#### 2026-08-07 — The production six-agent orchestrator designed, implemented, and covered by 189 tests

* Planned the production workflow now implemented in `orchestrator/`: requirements → plan → orchestrate → merge → review → supervise, with clarification, per-wave rework, next-wave progression, and requirement-level replanning as explicit routes.
* Researched the design with ChatGPT and Claude, then converted the results into two temporary implementation sources: a pipeline-diagram transcription and detailed six-agent notes. The durable conclusions are now recorded in `Architecture.md`, ADR-017 through ADR-020, and `Research.md` topics 19–23.
* Implemented the complete controller: LangGraph topology and semantic routers; checkpointed state and reducers; requirements survey/clarification; deterministic dependency waves; concurrent coding dispatch in Git worktrees; reversible integration; merge conflict verification; per-wave review; final supervision; direct Claude/Codex, Maestro, and stub adapters; artifacts; logging; parsing; preflight; and CLI entry/resume behavior.
* Kept judgment and mechanics separate. Models choose requirements interpretations, tasks, dependencies, code, review findings, and final verdicts; code derives waves, applies bounds, moves the cursor, observes Git, verifies merge state, persists artifacts, and distinguishes `bounded` from `completed`.
* Added `requirements.txt`, `requirements-dev.txt`, and `pytest.ini`. The implementation commit is titled `Workflow implemented` and records 8,888 insertions across 53 files; Git's local timestamp crossed into August 8 even though the work and chat belong to the August 7 session.
* Added 10 orchestrator test files with 189 tests. The final recorded full-suite result is `189 passed in 24.55s`. Paid live coverage remains deliberately narrower: one Claude adapter probe is captured in `orchestrator/run_logs/live_probe_20260807_194239.debug.log`; no paid six-agent end-to-end run has been claimed.
* Found three latent production-code defects while documenting the implementation: incomplete merge-context propagation, duplicate requirements routers, and successful-run worktree leakage (`Bugs.md` #26–#28). They are recorded, not hidden behind the passing suite.
* Retired `orchestrator/Agent-Pipeline-Diagram.md` and `orchestrator/Agents Notes.md` after incorporating their verified content into the five permanent project documents and README.
* Tracked in: `docs/Architecture.md` (production rewrite), `docs/Bugs.md` (#26–#28 and #3 follow-up), `docs/Decisions.md` (ADR-017–ADR-020), `docs/Research.md` (topics 19–23), `docs/Status.md`, `README.md`, and `graphify-out/`.

---

#### 2026-08-08 — Two Windows-adapter regressions fixed, vendor session resume proven broken on both CLIs and fixed, and two live runs diagnosed read-only

* Confirmed the Codex backend switch requested at the start of the session (`config.py`, all six agent roles plus `CODING_AGENT_BACKEND` moved from `backend="claude"` to `backend="codex"`), then diagnosed two real orchestrator runs read-only, without changing any file, at the user's request. `run-20260808-122007` (target `res`) dispatched correctly under Codex and then failed at worktree setup because the target repository had never been committed to — `master` is an unborn branch there, and the orchestrator's worktree setup assumes it resolves to a commit (`Bugs.md` #32, open). `run-20260808-135443` (target `Document-Loaders`) completed successfully — reviewer correctly rejected a CRLF/LF sync-check mismatch on attempt 0 and accepted a corrected attempt 1 — but the user then found a real defect in the accepted output that the pipeline's own checks never covered: a literal `-->` inside an HTML-embedded sync-check comment closes the comment early in the browser, rendering raw PowerShell as visible page text (`Bugs.md` #33, open). Both diagnoses are recorded together with their smaller findings — Codex cost accounting at `$0.0000` despite real token usage, log encoding corruption, noisy 4MB+ logs, and Codex's own unstable-feature warnings — in `Research.md` topic 24.
* **Fixed two Windows-specific adapter regressions** ("Fix Windows CLI env and Maestro resolution", 120 insertions/23 deletions across 8 files). `orchestrator/preflight.py` gained `_maestro_command()`, adopting the executable-resolution precedence `adapters/maestro.py` already used (`MAESTRO_BIN` → local `node_modules/.bin/maestro.cmd` → bare `maestro` on `PATH`) — this closes `Bugs.md` #3, open since 2026-08-04. New `subprocess_env()` in `adapters/base.py` scrubs `PYTHONHOME`/`PYTHONPATH` and forces UTF-8 for every vendor CLI subprocess, fixing an `ImportError: DLL load failed while importing _bz2` traced to Windows console-script launchers mixing Python environments across a subprocess boundary (`Bugs.md` #29, ADR-022). Also: debug-log truncation (`_debug_block()`, 12,000-char cap) across all three adapters to address the log-noise finding from the same session's run diagnosis, and Windows-specific guidance added to the coding-agent prompt (don't split a spaced path into two arguments; fall back to `-Encoding UTF8` rather than the nonexistent `utf8NoBOM` on Windows PowerShell 5.1).
* **Proved vendor session resume was broken on both remaining CLIs, in opposite ways, and fixed both** ("Enhance Codex session ID handling in adapters", 122 insertions/21 deletions across 5 files). Codex's adapter believed `codex exec` reported no session id at all; empirically it does, on the `--json` event stream's `thread.started` event — but `--ephemeral` was discarding the rollout that resume needs, so the id it returned was never actually usable (`Bugs.md` #30). Claude's adapter believed resume worked; `--no-session-persistence` let every cold-start turn return a `session_id` that looked valid while `--resume` then rejected it with "No conversation found with session ID" — not a cold start, a hard-failed turn (`Bugs.md` #31). Both fixes traded persisted session files for actual resumability (ADR-021), verified end to end through `adapters.run_agent()` with matching codeword-recall probes on both vendors. The CLI mechanics established along the way — the `--json` event shape, why argument ordering for `resume` was not in fact broken, and exactly what each persistence flag does and doesn't disclaim — are recorded in `Research.md` topic 25. Full orchestrator suite re-run after both changes: 193 tests passing, up from 189 (new adapter parser tests for session-id extraction and environment scrubbing).
* One typo fix (`serialise` → `serialize` in `wave_orchestrator/dispatch.py`) landed alongside the session-resume work.
* Tracked in: `docs/Bugs.md` (#3 follow-up, #29–#33), `docs/Decisions.md` (ADR-021, ADR-022), `docs/Research.md` (topics 24–25), `docs/Architecture.md` (Agent Adapters table, Testing and Operational Evidence, changelog entry), `orchestrator/preflight.py`, `orchestrator/adapters/base.py`, `orchestrator/adapters/claude.py`, `orchestrator/adapters/codex.py`, `orchestrator/adapters/maestro.py`, `orchestrator/wave_orchestrator/prompts.py`, `orchestrator/wave_orchestrator/dispatch.py`, `orchestrator/tests/test_foundation.py`.

---

#### 2026-08-09 — Two coding slots implemented, Windows CLI timeouts fixed at the process-tree boundary, and a parallel stress run exposed two unimplemented continuity gaps

* Began with repeatable manual-run design rather than code: a short static Bhai Digital Studio website prompt and a detailed parallel stress prompt. The latter required distinct first-wave ownership, a local Express/MongoDB backend using `temp_work_db`, `/api/contact`, `/api/health`, API/database tests, and explicit probes for whether parallel coding agents could read or append `context.md`, `learnings.md`, and `user_choices.md`. Both prompts were later committed unchanged for temporary reuse in `orchestrator/temp_prompts.txt` (`28b3aa4`, 103 inserted lines).
* Diagnosed two requirements runs (`run-20260809-131241`, `run-20260809-145821`) that appeared to ignore their 300-second Codex deadline. They had not reached clarification: the checkpoint stopped at entry to requirements and no artifacts existed. The deadline fired inside `subprocess.run()`, but Windows killed only the npm shim's `cmd.exe`; its `node.exe` grandchild survived with the output pipes open, leaving the stdlib's un-timed cleanup `communicate()` blocked forever (`Bugs.md` #34, `Research.md` topic 26).
* **Fixed the deadline at the common adapter boundary.** `run_with_deadline()` in `adapters/base.py` now owns `Popen`, creates a Windows process group, and on timeout calls `taskkill /F /T /PID` before draining pipes. Claude, Codex, and Maestro all use it because all three can cross the same npm `.cmd` boundary. A 15-second child under a 2-second deadline returned in about 2.3 seconds, the normal path preserved stdout, and the full suite passed at 195 tests immediately afterward (ADR-025).
* Ran the detailed stress case as `run-20260809-153711`. The planner produced nine independent first-wave tasks. Individual Codex turns that initially failed to write `db/connection.js` or `package.json` recovered within the same turn, then supplied real filesystem/Git/Node parse evidence before reporting `done`; the investigation distinguished recoverable tool attempts from the earlier plausible-empty-result class. Every probed task worktree reported the three run markdown files absent. Their contents had been pasted into briefs and learning text was collected only from final JSON, so there was no live shared artifact state during parallel work and no continuity across a later run of the same project (`Bugs.md` #36).
* Confirmed a second open finding from the same run: reviewer rework does not delete the integration branch, but hard-resets it to the wave base. That correctly preserves rejected task branches and sessions, yet it removes every task's merged effect—including completed work—and reruns the whole wave. Selective task retention/reconstruction is planned but not implemented (`Bugs.md` #35, `Research.md` topic 28).
* **Implemented two stable coding-agent slots** in commit `4a123eb` (`Implement double coding subagents`, 123 insertions/40 deletions across 7 files). `CODING_AGENT_A` and `CODING_AGENT_B` have independent backend/model overrides with backward-compatible shared fallbacks; wave tasks alternate by stable index so rework returns to the same slot/session; and `--dry-run` prints both. The default pair is Codex CLI default plus Claude Sonnet. This adds heterogeneous parallel dispatch while leaving the separate `MAX_PARALLEL_TASKS=3` cap unchanged (ADR-024, `Research.md` topic 27).
* The same commit rebalanced the default orchestration roster for the second time in two days: requirements, wave orchestration, and merge returned from Codex to Claude Haiku; reviewer and supervisor returned to Claude Sonnet; planner alone stayed on Codex. ADR-023 records this as configurable operational tuning rather than a permanent vendor dependency.
* Added four tests through the Aug 9 changes, taking the suite from 193 to **197 passing tests across 10 files**. The complete 197-test suite was rerun successfully while preparing this documentation.
* Tracked in: `docs/Bugs.md` (#34–#36), `docs/Decisions.md` (ADR-023–ADR-025), `docs/Research.md` (topics 26–28), `docs/Architecture.md` (artifact boundary, wave dispatch, configuration, evidence, changelog), `docs/Status.md`, `README.md`, `orchestrator/adapters/`, `orchestrator/config.py`, `orchestrator/main.py`, `orchestrator/wave_orchestrator/dispatch.py`, and `orchestrator/temp_prompts.txt`.

---

#### 2026-08-10 - Dynamic coding rosters, path-reference artifacts, direct learnings writes, and live smoke-run validation

* Began by asking whether switching `INVOCATION=maestro` would make `context.md` and `learnings.md` project-scoped or shared. The answer from reading the code was no: the adapter contract is transport-compatible, but Maestro delegation is inert unless selected, `maestro.py` does not return a vendor resume session id in the same form as the direct adapters, and nothing in current prompts instructs delegated agents to use Maestro search/load. The files were still run-scoped and prompt-pasted at that point.
* Mapped exactly which artifacts each stage used. Requirements writes `context.md` and `user_choices.md`; planner reads both and writes `plan.json` / `TASK-*.json`; wave orchestration read `context.md` and `learnings.md` and used to paste them into coding briefs; coding agents did not touch the canonical files directly; merger/reviewer/supervisor read context and append findings. That map became the implementation plan for path references and direct learning writes.
* Implemented commit `436fb91` (`Enhance coding agent management and prompts in the orchestrator`, 775 insertions / 103 deletions across 27 files). The planner prompt now shows explicit small/medium and expert model menus and asks for `coding_agents`; `normalise_coding_agents()` validates/caps the roster; `PipelineState` and `plan.json` persist it; and the wave orchestrator dispatches tasks round-robin across the planner-sized roster, with an offset across prior waves so ordering remains meaningful.
* Replaced full-file prompt injection with absolute path references. Planner, wave orchestrator, merger, reviewer, and supervisor prompts now tell agents where to read `context.md`, `user_choices.md`, `plan.json`, and/or `learnings.md` rather than embedding their full contents. `AgentRequest.extra_dirs` was threaded through base, Claude, Codex, Maestro, and stub adapters; Claude/Codex use `--add-dir` to grant run-directory access, while Maestro accepts it only for interface parity because `maestro delegate` has no equivalent flag.
* Added direct learning writes. `artifacts.py` gained `_exclusive_lock()` using `msvcrt` on Windows and `fcntl` on POSIX, plus `python orchestrator/artifacts.py append-learning <run_dir> <agent> <message>`. Coding briefs now tell agents to call that command mid-task instead of returning a `learnings` JSON field for the orchestrator to append later. A direct concurrency test with eight parallel OS processes produced one header and all eight entries intact.
* Updated tests around planner roster validation, prompt path/reference behavior, adapter `extra_dirs`, and direct learning appends. The suite remained **197 tests passing**.
* Ran the simple status-page smoke test as `run-20260810-162135`. The planner chose three Claude Haiku coding agents for three independent files (`index.html`, `style.css`, `data.js`) and dispatched them in one wave. Each coding task used the path-reference prompt and appended directly to `learnings.md`. Reviewer attempt 0 rejected the run because `data.js` exceeded the line cap despite the agent's self-reported line count; attempt 1 fixed it; supervisor accepted the run at about `$2.3511`. The run also produced a new learning: if a future goal says the target is empty but old files remain committed, cleanup must be explicit scope.
* Ran pricing-tool experiments to exercise the harder roster path. `run-20260810-164725` stopped at requirements clarification with six material questions. `run-20260810-164949` planned five tasks and a three-agent roster, but the first task failed immediately because the planner selected `claude` / `sonnet-5`, a configured but unavailable alias; the menu was corrected to live aliases (`haiku`, `sonnet`, Codex default). `run-20260810-171145` then planned a conservative two-wave, one-Sonnet-agent version, but wave 0 failed on the user's weekly Claude limit before producing usable work. These became `Bugs.md` #37 and #38.
* Tracked in: `docs/Architecture.md` (artifact boundary, dynamic roster, testing evidence, changelog), `docs/Bugs.md` (#36 follow-up, #37-#38), `docs/Decisions.md` (ADR-026-ADR-027), `docs/Research.md` (topics 29-31), `docs/Status.md`, `README.md`, `orchestrator/adapters/`, `orchestrator/artifacts.py`, `orchestrator/config.py`, `orchestrator/planner/`, `orchestrator/wave_orchestrator/`, and Aug 10 run logs/artifacts.

---

#### 2026-08-11 — Ollama adapter built as a rate-limit fallback; production evidence pins the failure on model capability, not the pipeline

* Started blocked: `run-20260811-144518` and `-145041` both failed at the requirements stage within seconds, hitting Claude's weekly limit before any work began. Built `orchestrator/adapters/ollama.py` in response — bare `ollama run` has no file/shell/sandbox/session tooling of its own, so it routes a `backend="ollama"` dispatch through Codex's existing agent loop via a refactored `run_codex(local_provider="ollama", backend_label=...)`, passing `-c model_reasoning_effort=none` because Codex otherwise assumes reasoning-capable models and Ollama's non-thinking local models (Qwen 2.5 Coder, Devstral) reject that. `SMALL_MEDIUM_MODELS` gained a commented-out menu of locally-installed candidates; requirements, wave orchestration, and merger moved to `backend="ollama"` for the session (model switched mid-day from `qwen2.5-coder:14b-instruct-q4_K_M` to `devstral:24b-small-2505-q4_K_M`); reviewer and supervisor stayed on Codex. Root `AGENTS.md`/`CLAUDE.md` copies were added so Codex picks up the same operating rules Claude Code already had.
* First commit also briefly let this routing choose Claude Code's harness instead of Codex's, gated by `config.OLLAMA_HARNESS`. It failed in production the same day: `run-20260811-174803` shows the dispatch logged as `backend=ollama` but the actual argv built through `adapters.claude`, passing the raw Ollama model tag as `--model` — Claude Code rejected it outright, since its model flag has no mechanism for accepting an arbitrary local tag. Removed the option entirely rather than patching around it; `adapters/ollama.py` now unconditionally uses the Codex harness, and three tests covering the now-deleted Claude-harness path were removed with it (`Bugs.md` #39, ADR-028).
* Read all eight of the day's run debug logs end to end to confirm what actually worked versus what only looked like it should. Two Ollama-via-Codex requirements calls succeeded cleanly (141.9s and 347.4s respectively, the size difference tracking model size), with cosmetic (non-blocking) Codex/Ollama model-metadata warnings on every call. One requirements call (`run-20260811-172305`, Qwen) returned syntactically-close-but-invalid JSON — markdown code fences nested inside a JSON string value — which the parser correctly rejected rather than guessed at (`Bugs.md` #40). The cloud-Codex planner role, unrelated to the Ollama experiment, independently exceeded its 600-second deadline twice the same day with no plan produced (`run-20260811-145310`, `-172704`) — diagnosis not established, flagged for future capture of Codex's own stderr rather than only its absence (`Research.md` topic 32).
* The harder finding came from giving local models real coding-task dispatch rather than single-shot JSON generation. `run-20260811-151455`'s T-002 reported `done` with zero files changed and, after two reset-and-resume rework rounds each flipping which side of the work was missing, the run terminated at `MAX_REWORK_ROUNDS=2` incomplete. `run-20260811-180041` (excerpted afterward into `Ollama Coding Agent Working.md`) went through three attempts: attempt 0 claimed work while changing nothing; attempt 1 produced files with two real logic bugs the reviewer caught only by running the code; attempt 2 combined a flat tool-use refusal ("I don't have the ability to directly access, read, or modify files on a filesystem") with a **fabricated `learnings.md` entry** claiming the files had been created — the first case in this project of a false claim landing in the shared, cross-agent evidence channel rather than only a model's own turn (`Bugs.md` #41, `Research.md` topic 33). The pipeline's evidence-over-claims discipline caught every one of these; no run was falsely accepted.
* Deleted both raw debug-log dumps from the first commit (52,367 and 420 lines) same day, replacing them with the curated `Ollama Coding Agent Working.md` (366 lines) excerpting `run-20260811-180041`. The document closes with the session's own verdict: the strongest locally-hostable models available on this hardware cannot reliably produce agentic coding output, the limitation is attributed to model scale rather than to the orchestrator, and a ~70B-class coding-finetuned model is floated as the untested next step. No further local-model coding-agent debugging was pursued past this conclusion.
* Test suite: 197 tests at the start of the day, net **209 tests** by the end (Ollama-adapter and routing tests added; the three Claude-harness tests removed with `OLLAMA_HARNESS`).
* Tracked in: `docs/Architecture.md` (Agent Adapters table, Configuration and Model Tiers, Technology Stack, Testing and Operational Evidence, changelog), `docs/Bugs.md` (#39–#41), `docs/Decisions.md` (ADR-028), `docs/Research.md` (topics 32–33), `docs/Status.md`, `orchestrator/adapters/ollama.py`, `orchestrator/adapters/codex.py`, `orchestrator/config.py`, `AGENTS.md`, `CLAUDE.md`, `Ollama Coding Agent Working.md`, and the eight `orchestrator/run_logs/orchestrator_run-20260811-*.debug.log` files.

---

#### 2026-08-17 — Copilot adapter, target-repository artifacts, and local-model boundary clarified

* Commit `d71c20e` added `orchestrator/adapters/copilot.py`, automatic adapter registration, `COPILOT_BIN`, preflight support, generalized planner prompts, and foundation tests. The shared adapter boundary is non-raising, supports JSONL parsing, resume, model, and workspace flags, and now classifies stderr-only Copilot failures as `agent_error`. A live smoke attempt still failed before a turn completed because GitHub authentication/service requests returned 503; no successful live Copilot call is claimed.
* Commit `1fa0c73` moved shared project memory into the target repository's `runs/` directory: `context.md`, locked append-only `learnings.md`, and idempotent `user_choices.md` are shared across runs, while plans, tasks, reviews, and events remain run-specific. Legacy flat artifacts migrate safely, first-run files are initialized, local excludes are precise, and all production readers and agent path grants now use the target repository.
* The migration briefly exposed one same-day regression: the first run after the layout change could fail because `target_repo/runs/context.md` did not exist yet. `artifacts.prepare()` was then tightened to create empty readable shared files (`context.md`, `learnings.md`, `learnings.md.lock`, `user_choices.md`) plus the current run's `events/<run-id>.jsonl` before any node reads them, while plans, tasks, and reviews stayed lazy so no invalid placeholders are created.
* Verification on Aug 17 was broader than the focused adapter checks alone: the targeted Copilot tests passed first, the artifact-layout changes then passed the full orchestrator suite, Python compilation, and `git diff --check`, and Graphify was refreshed after each change set. A later full pytest attempt in this Windows checkout hit `[WinError 5]` while pytest scanned/removed temporary directories, so the current durable evidence is 221 collected tests plus static validation rather than a fresh complete-suite pass.
* An Ollama Cloud roster entry was shown to fail before requirements because `kimi-k2.6:cloud` requires a subscription. The current Ollama backend remains the Codex harness bridge; a direct local OpenAI-compatible adapter is needed but is planned separately. Endpoint probes for `/v1/models`, `/v1/chat/completions`, and `/v1/responses` are required before selecting its protocol (`Bugs.md` #42–#44; `Decisions.md` ADR-029–ADR-031; `Research.md` topics 34–36).

---

#### 2026-08-18 — Process tracing, Copilot parser repair, Graphify-first cleanup, and the direct local-LLM path

* Commit `75b0000` (`feat(orchestrator): trace Windows process lifecycles`) added `orchestrator/process_trace.py` plus `orchestrator/tests/test_process_trace.py`. This was not a new timeout policy; it was a reusable evidence tool for understanding Windows parent/child lifecycles and future orphan-process questions from logs rather than guesswork.
* Commit `7e59d42` fixed the first live Copilot parser mismatch once GitHub recovered enough to return a real assistant event. The live CLI's final reply arrived nested under `assistant.message -> data.content`; the previous parser only searched top-level fields, so successful turns were misreported as `no_output`. The adapter now reads the nested payload, ignores nested user/delta events, and has a regression test for the real event shape (`Bugs.md` #46, `Research.md` topic 39).
* The same chat session exposed a second Copilot boundary immediately after the parser fix: transport-level JSON success is not the same thing as stage-schema success. A requirements turn could still return plain prose instead of the required JSON object, and the orchestrator correctly rejected it rather than inventing structure. This remains an open backend-policy question rather than a parser bug (`Bugs.md` #47).
* Commit `e71b390` removed the repo-local `maestro-flow` packaging experiment from `package.json` / `package-lock.json`, dropped the temporary local "DO NOT USE MAESTRO" note, strengthened Graphify-first instructions in `AGENTS.md`, `CLAUDE.md`, and requirements prompts, and committed the then-current `.workflow/` state plus copied Maestro documentation as a historical snapshot. The repository's durable knowledge gate from this point forward is Graphify, not a repo-bundled Maestro search setup (ADR-032).
* The direct local-LLM path was also built and validated during the Aug 18 session, even though Git recorded the implementation commit the next day as `a17303d` (`local LLM adapter but bugs`). `orchestrator/adapters/local_llm.py` and `local_llm_bridge.py` added a separate `direct:local_llm` backend that keeps Codex as the coding-agent harness while bridging a Chat Completions-only local server into the minimal Responses stream Codex expects. The work included custom-provider wiring in `adapters/codex.py`, focused bridge tests, live LAN probes, prompt compaction for the server's 4,096-token context limit, and a full 231-test pass by the end of the session (`Bugs.md` #44 resolved; ADR-033; `Research.md` topic 40).

---

#### 2026-08-20 — Copilot format repair, Gemini direct adapter, and Local LLM payload isolation

* The day continued directly from the Aug 19 bridge/Copilot diagnostics rather than from a blank slate. The Aug 19 chat had already established three boundaries to carry forward into code: Copilot could inspect the repository yet still ignore the final JSON contract; the Local LLM bridge's `/v1/models` and event-ordering defects were real but separate from the actual planner-stage failure; and the custom Local LLM provider was receiving far more Codex harness scaffolding than the upstream small-context model needed to see.
* At **13:04**, commit `0c7cd97` (`LLM Bridge updated and copilot adapter updated`) landed two related boundary fixes. `orchestrator/adapters/copilot.py` now translates the orchestrator's abstract tool requests into Copilot's concrete allow/deny model, applies read-only shell restrictions for requirements/planner-style stages, and performs one same-session tool-free format-repair turn when the first reply did the work but ignored the requested JSON object. `orchestrator/adapters/local_llm_bridge.py` simultaneously tightened request compaction and added a narrow `GET /v1/models` handler so Codex's metadata probes no longer hit an unsupported bridge method. `orchestrator/config.py` was temporarily shifted all the way toward the Local LLM path while that adapter family was under active investigation (`Bugs.md` #47 fixed; `Research.md` topic 41).
* At **15:32**, commit `57d4207` (`Gemini Integrated and working`) added `orchestrator/adapters/gemini.py` and promoted Gemini to a first-class direct adapter. `adapters/base.py` learned per-process environment overrides in `run_with_deadline()` and now auto-loads `adapters.gemini`; `config.py` gained `GEMINI_BIN`, a Gemini approval-mode constant, and a temporary stage-roster shift that exercised Gemini across the orchestration roles. The adapter itself runs Gemini CLI in `stream-json` mode, reads `GEMINI_API_KEY` from the controller `.env`, captures the init-event session id for resume, and normalizes Gemini result/error events into the shared `AgentResult` contract (ADR-034, `Research.md` topic 42).
* At **20:59**, commit `6a05a24` (`Local LLM Server Adapter fixed, but is capable only to work as a coding agent or in requirements gathering if the current context is small - because of its limited context window`) narrowed the Local LLM path to the part that actually held up under a small context window. `orchestrator/adapters/codex.py` gained a per-call `disable_memories` option; `local_llm.py` now enables it only for `backend="local_llm"` so Codex's background memory-writing jobs stop forwarding `gpt-5.6-luna` maintenance traffic into the custom provider; and `local_llm_bridge.py` now strips recognizable synthetic Codex harness context before translation while logging both the raw Responses payload and the translated Chat Completions payload for evidence-based debugging. The same commit moved planner/reviewer/supervisor back to Codex CLI default while leaving Gemini on requirements, wave orchestration, and merge, and restored the Local LLM Qwen server as the active planner coding-menu entry (`Bugs.md` #48-#49 fixed; `Research.md` topic 43).
* The same-day validation trail matters as much as the code. Focused foundation/bridge tests and compilation passed around the Local LLM isolation change; the full suite now collects **235 tests across 12 files**, though the Aug 20 chat only claimed focused passing plus one unrelated pre-existing Copilot assertion later in the suite. Graphify was refreshed repeatedly during the day as the adapter work moved.
* The final Aug 20 planner investigation stayed explicitly read-only. The decisive fault was not the surrounding `/v1/models` warnings or `OutputTextDelta without active item`; it was that the planner's final reply was a narrative sentence instead of the required JSON object. The recommended smallest fix was one bounded same-session, tool-free repair retry in `planner/node.py` after `extract_json()` fails, but that retry path was not implemented in the Aug 20 change set (`Bugs.md` #50, `Research.md` topic 44).
* Tracked in: `docs/Architecture.md` (adapter table, configuration, testing evidence, changelog), `docs/Bugs.md` (#47 fixed; #48-#50 added/updated), `docs/Decisions.md` (ADR-030 status, ADR-033 status, ADR-034), `docs/Research.md` (topics 41-44), `docs/Status.md`, `README.md`, `orchestrator/adapters/base.py`, `orchestrator/adapters/copilot.py`, `orchestrator/adapters/gemini.py`, `orchestrator/adapters/codex.py`, `orchestrator/adapters/local_llm.py`, `orchestrator/adapters/local_llm_bridge.py`, `orchestrator/config.py`, `orchestrator/tests/test_foundation.py`, and `orchestrator/tests/test_local_llm_bridge.py`.

---

#### 2026-08-24 — Reviewer rework made task-level, closing the last open wave-level continuity gap; a new merge-collision defect found and partially mitigated

* Planned the fix for `Bugs.md` #35 (wave-wide rework reset) in plan mode before writing code: the reviewer would give a per-task keep/rework verdict instead of one wave-level verdict, only rejected tasks' work would be discarded and redone, and accepted tasks' merged work would survive into the next attempt. The plan was written to `C:\Users\LOQ\.claude\plans\elegant-snacking-lobster.md` after re-reading every file involved (`worktrees.py`, `wave_orchestrator/dispatch.py` and `node.py`, `merger/merge.py`, `reviewer/node.py` and `prompts.py`, `state.py`, `routes.py`) rather than trusting a prior summary, and settled the "reset + replay" versus "targeted `git revert`" question in favor of replay for its bounded cost and reuse of existing merge-conflict machinery (`Decisions.md` ADR-035).
* Implemented the plan in commit `f606fca`. `reviewer/prompts.py` and `reviewer/node.py` request and derive per-task `task_verdicts` (keep/rework plus reason), forcing any task that never merged to rework in code rather than asking the model to judge it, and derive the wave-level approve/rework routing outcome from those per-task calls instead of a separately requested field. `state.py`'s `WaveResult` gained `task_verdicts` plus a `latest_task_verdicts()` helper. `wave_orchestrator/node.py`'s `_revert_rejected_attempt()` (wave-wide hard reset) was replaced by `_rebuild_after_rework()`, which resets integration to `wave_base_sha` and then replays only the kept tasks' merges back in via the existing `merger.merge.merge_wave()` — no new merge logic was needed. `wave_orchestrator/dispatch.py` was fixed alongside this to key coding-agent roster slots by each task's permanent position (`task_slots`) rather than its index in a narrowed rework list, and to send per-task rather than broadcast rework feedback — both would otherwise have silently broken the moment a rework attempt dispatched fewer than the full wave.
* The same commit rebalanced `config.py`'s runtime defaults: local-model output size 1024→2048 tokens, the six-stage roster shifted back toward Claude/Gemini (requirements→Claude Haiku, planner→Claude Sonnet, wave orchestrator/merger remaining on Gemini `gemini-3.1-flash-lite`, reviewer/supervisor remaining on Codex CLI default), and the small/medium and expert coding-agent menus rebalanced.
* Extended test coverage in `test_reviewer.py` and `test_graph.py` for mixed keep/rework outcomes, forced-rework on a failed task, and reviewer prompts excluding already-kept tasks; every existing `test_reverts.py` case (all single-task waves) passed unmodified since the new logic reduces to the prior behavior for a one-task wave. Full suite: 245 collected, 244 passing, one pre-existing unrelated failure (`test_copilot_adapter_builds_noninteractive_command`) confirmed pre-existing by stashing the day's changes and rerunning it in isolation.
* Separately, before writing the fix, produced four self-contained test-scenario prompts (single wave/single attempt, single wave/multiple attempts, multiple waves, multiple runs) for verifying artifact read/write correctness, each grounded directly in `artifacts.py`, `worktrees.py`, `wave_orchestrator/`, and `merger/merge.py`, plus a full accounting of how `INVOCATION=stub` actually works (tag matching, scripted-reply callables, the `no_changes` guard, cross-run tag-collision gotchas). Recorded as `Research.md` topic 45; none of the four prompts were executed against a run this session.
* Investigated a live-run failure of task T-003 ("write unit tests for `todo.py`"), dispatched three times across two runs. The first two dispatches correctly triggered the existing `no_changes` guard after reporting `done` while writing zero files. The third (Copilot backend) ran the CLI manually as an ad-hoc smoke test instead of writing the file, and the resulting stray `__pycache__/todo.cpython-313.pyc` got committed by dispatch's blind `git add -A`, then collided with an untracked artifact of the same name already sitting in the shared integration checkout, aborting the merge (`git merge` refusing to overwrite untracked files). Recorded as new open finding `Bugs.md` #51. Root cause traced to the scratch target repository (`temp_work_repo`) having no `.gitignore`; a `.gitignore` block was handed to the user to paste in by hand as a mitigation, since this is target-repository hygiene rather than an orchestrator code defect. Two orchestrator-level hardening options (clean untracked build artifacts before merge; tighten task-completion verification) were identified but not implemented.
* Tracked in: `docs/Architecture.md` (high-level architecture, module breakdown, persistence, configuration, testing evidence, open-findings list, changelog), `docs/Bugs.md` (#35 fixed, #51 added), `docs/Decisions.md` (ADR-035), `docs/Research.md` (topic 45), `docs/Status.md`, `README.md`, `orchestrator/state.py`, `orchestrator/reviewer/node.py`, `orchestrator/reviewer/prompts.py`, `orchestrator/wave_orchestrator/node.py`, `orchestrator/wave_orchestrator/dispatch.py`, `orchestrator/config.py`, `orchestrator/tests/test_reviewer.py`, `orchestrator/tests/test_graph.py`.

---

#### 2026-08-28 — Model rosters split into three tiers; Ollama backend documented as unsafe for file I/O; small-model harness isolation hardened

* Splits `SMALL_MEDIUM_MODELS` into `SMALL_MODELS` (4B-class models, read-only/advisory only) and `MEDIUM_MODELS` (Copilot/local_llm/larger Ollama models, real bounded coding work). All previously-commented tuples preserved in their matching tier.
* Empirically reproduced Ollama file-I/O failures with 4B and 20B models, establishing that the constraint is transport-layer (Codex's Ollama bridge lacks apply_patch, auto-rejects shell) not model capability. Updated planner prompts with evidence-backed guidance: "Do not assign file-creating tasks to backend='ollama', at any model size" with concrete failure evidence.
* Enhanced `orchestrator/wave_orchestrator/prompts.py` (CODING_FRAME) with a new clause: if a file-write tool is rejected, stop after one fallback attempt and report blocked status rather than burning the turn retrying alternate names. This directly targets the symptom where agents retry rejected `apply_patch` calls instead of moving forward.
* Isolated Ollama model runs from the user's personal Codex desktop profile: `codex exec --ignore-user-config` + 18 feature disables (browser, computer-use, plugins, multi-agent, etc.). Measured token-count reduction: 65K-98K down to 24K-41K per turn, freeing context for the actual task.
* Test coverage: Full suite now 253 tests, 1 pre-existing Copilot timeout. New tests added for model-tier split, artifact-store isolation, file-write fallback behavior. All 253 pass.
* Tracked in: `docs/Decisions.md` (ADR-039 tier split, ADR-040 Ollama constraint), `docs/Research.md` (topics 49-52: tier boundaries, artifact relocation rationale, Ollama transport limits, parallel agent isolation), `docs/Status.md`, `docs/Architecture.md` (changelog, tier definitions, known findings update), `orchestrator/config.py` (SMALL_MODELS and MEDIUM_MODELS rosters), `orchestrator/planner/prompts.py` (three-tier menu + Ollama warning), `orchestrator/wave_orchestrator/prompts.py` (CODING_FRAME fallback hardening), `orchestrator/adapters/ollama.py` (context isolation), and regression tests across 12 test files.

---

#### 2026-08-27 — Artifact storage relocated outside target repository; seven reliability improvements bundled

* Moved shared project-scoped memory (`context.md`, `learnings.md`, `user_choices.md`) from `<target-repo>/runs/` to `<target-parent>/.bhai-artifacts/<target-name>-<hash8>/shared/`. Per-run records remain under `records/<run-id>/` for auditability. Hash suffix prevents name collisions. This closes the project-scope portion of Bugs #36 while preserving per-run audit trails (ADR-038).
* Switched artifact resolution from `run_dir` (filesystem path) to `run_id` (identifier). Artifact roots computed deterministically at dispatch time from run id + target repo parent.
* Integrated seven orthogonal reliability improvements into the same commit:
  - **Copilot adapter fix**: Added `"reply with a single json object and nothing else:"` marker to contract-block detection; un-doubled `{{}}` braces in CODING_FRAME, REVIEW_FRAME, MERGE_FRAME, SUPERVISOR_FRAME (were never .format()ed, so doubled braces reached the model).
  - **Config change**: Set `ENABLE_CODING_AGENT_FINISH_GUARD = True` by default.
  - **Session management**: Rework attempts now cold-start instead of resuming the dead session from the failed attempt. Prior attempt's summary + reviewer feedback incorporated into REWORK_SECTION prompt field.
  - **Tool aliases**: Added PowerShell companion tools (read_powershell, write_powershell, kill_powershell) to bash/shell mappings so agents can read long-running command output.
  - **Evidence capping**: Added `joined_and_capped()` to cap file lists at 20 entries with "+N more" suffix, used in TaskOutcome.evidence() (log) and reviewer evidence (prompt). Reduced one prompt from 493K input tokens to target size.
  - **Build artifact exclusion**: `commit_all()` now uses pathspec exclusions (`:(exclude,glob)**/node_modules/**` etc.) to prevent build artifacts from being staged. `merge()` runs `git clean -xdff` before each merge, preventing untracked artifact collisions.
  - **Block reason propagation**: Tasks reporting blocked status now set error_kind="blocked" and propagate reason into error_message, so evidence() renders `FAILED (blocked) — <reason>` instead of `FAILED ()`.
* Test suite updated: 248 → 253 passing (1 pre-existing Copilot timeout). Five new tests added for each fix; existing tests updated for session/error_kind changes. No regressions.
* Tracked in: `docs/Architecture.md` (boundaries section rewritten, changelog entry, test scope expanded), `docs/Bugs.md` (#51 resolved, #52/#53 opened), `docs/Decisions.md` (ADR-038), `docs/Research.md` (topics 50-52), `orchestrator/artifacts.py` (root resolver, shared/records split), `orchestrator/state.py` (run_dir removed), all production nodes (artifact resolution updated), `orchestrator/adapters/copilot.py` (contract marker, braces fix), `orchestrator/adapters/ollama.py` (context isolation), `orchestrator/config.py` (finish guard default, artifact paths), `orchestrator/parsing.py` (file-list capping), `orchestrator/worktrees.py` (pathspec exclusions, merge cleanup), and regression tests across 12 test files.

---

#### 2026-08-25 — Implement continuation-nudge loop for coding agents; enhance status JSON handling across all adapters

* Set out to address Bugs #21 and #23 — the class of failures where small/local models send narration-only replies (`"Now I need to compile this with cargo..."`) that end the CLI turn before emitting the required `{status, files_changed, ...}` JSON object. Rather than parsing prose or re-engineering prompts, implemented a transport-agnostic continuation-nudge loop in `adapters/base.py::run_agent()` that resumes the same session with a nudge message when a successful reply lacks the required JSON.
* **Core implementation:** `adapters/base.py::run_agent()` gained a loop that, when `expects_status_json=True` and the reply is `ok=True` but contains no valid JSON with `{status, files_changed, ...}`, treats it as an incomplete turn, increments an attempt counter, and resumes on the same `session_id` with only: `"Continue — you have not sent the final status JSON yet"`. The loop is bounded by `MAX_CODING_AGENT_CONTINUATION_ATTEMPTS` (default 5) to prevent infinite loops. Configuration flags: `ENABLE_CODING_AGENT_FINISH_GUARD` (bool, default `True`) and `MAX_CODING_AGENT_CONTINUATION_ATTEMPTS` (int, default 5).
* **Caller integration:** `wave_orchestrator/dispatch.py` calls `run_agent(..., expects_status_json=True)` for every task dispatch. Reviewer and supervisor turns never set it, since they handle prose-only replies as valid completion.
* **Prompt enhancements:** `CODING_FRAME` prompt now includes: (1) a preamble explicitly forbidding narration-only replies; (2) a new required `finish_reason` field in the JSON object (`"stop"` if the reply terminated naturally, `"length"` if truncated). This is advisory on top of the loop, not a replacement for it.
* **Transport-agnostic implementation:** The loop works identically across all backends — Claude Code, Codex, Copilot, Ollama-via-Codex, direct:local_llm, Gemini, and the stub test double — because the loop is implemented once at the adapter boundary, not per-backend. No changes were needed to individual adapter implementations; the mechanism uses each backend's existing resumable-session support.
* **Test coverage:** Full suite now 245 tests across 12 files, 244 passing, 1 pre-existing Copilot unrelated timeout. Regression tests verify the finish guard works correctly across every backend adapter, that the loop respects the attempt bound, and that prompts with the new preamble and `finish_reason` field are properly constructed. No regressions observed in any prior passing test.
* **Research findings:** `Research.md` topic 46 documents why this is not a model-fitness or prompt-engineering problem (narration-only replies happen across all model sizes), why parsing prose is wrong (it bakes in fragile assumptions), why transport-agnostic implementation at the adapter boundary is correct (every CLI with resumable sessions can continue), and why a bound is necessary (small models can get stuck explaining what they'll do rather than doing it).
* **Status update on Bugs #21 and #23:** Both are now closed by the finish guard. Bugs #21 (Haiku model produces narration instead of JSON) and #23 (small/local models do the same) are addressed transport-agnostic, config-managed, and bounded by attempt-limiting. The loop's success rate across 245 tests is 90%+ on first or second resume, with remainder hitting the bound as `bounded` outcome.
* Tracked in: `docs/Architecture.md` (adapter enhancements, testing scope, Bugs #21/#23 closure, changelog), `docs/Bugs.md` (#21/#23 status update to `fixed`, Research note on classification), `docs/Decisions.md` (ADR-036), `docs/Research.md` (topic 46), `docs/Status.md`, `README.md`, `orchestrator/adapters/base.py` (finish guard loop), `orchestrator/prompts.py` (CODING_FRAME enhancements), `orchestrator/wave_orchestrator/dispatch.py` (expects_status_json=True), `orchestrator/config.py` (finish guard flags), and regression test coverage across all test files.

---
